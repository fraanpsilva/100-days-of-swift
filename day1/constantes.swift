/*
as constantes são idênticas as variáveis em todos os aspectos, com uma diferença imortante:
não podemos alterar seus valores depois de definidos. para criar uma constante usamos a 
palavra reservada LET, então basta ao invés de usar var, usar let
*/

let altura = 1.60 
let senha = "fr0sti3s"

// é preferível usar constantes sempre que possível