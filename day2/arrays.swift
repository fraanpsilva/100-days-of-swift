/* 
arrays servem para armazenar muitos valores, podemos criar constantes e 
variáveis como outros tipos de dados. lemos valores de arrays usando sua posição
numérica, contando a partir do 0.
Os arrays são ordenados (na ordem em que você os fornece) e podem conter valores duplicados dentro dele.
*/
// criando um array
var temperaturas = [32.0, 41.2, 36.5]
var medias = [98.5, 97.1, 99.9]
var cidades: [String] = ["Paris", "Londres", "Nova York"]
var leituras: [Bool] = [false, true, true, false]
print(temperaturas)

// criando array vazio
var nomes = Array<Int>()



